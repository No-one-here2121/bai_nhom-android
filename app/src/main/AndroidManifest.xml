<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo_thoi_tiet">
        <activity
            android:name=".Model.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".Model.MapActivity"
            android:exported="false" />
        <activity
            android:name=".Model.Lich_Activity"
            android:exported="false" />
        <activity
            android:name=".Model.ManHinhChinhLog"
            android:exported="false" />
        <activity
            android:name=".Model.ManHinhChinhNonLog"
            android:exported="false" />
        <activity
            android:name=".Model.Trang_dang_ki"
            android:exported="false" />
        <activity
            android:name=".Model.After_MoDau"
            android:exported="false" />
        <activity
            android:name=".Model.Started"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Model.Lv_thoi_tiet"
            android:exported="false" />

        <receiver
            android:name=".Model.Widget_thoi_tiet"
            android:exported="true"> <!-- Phải là true để hệ thống truy cập được -->
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_thoi_tiet_info" /> <!-- Trỏ vào file xml info -->
        </receiver>

        <receiver
            android:name=".Model.Bao_thuc_Receiver"
            android:exported="false" />

        <service android:name=".Model.Update_thong_tin_widget" />
    </application>

</manifest>